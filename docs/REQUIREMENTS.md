# アプリケーション要件定義

## プロジェクト名

**Commuter Pass Price Checker**（定期券価格チェッカー）

## 目的

通勤・通学において、**定期券を購入するか毎回きっぷを購入するか、どちらがお得かを判定する**Webアプリケーション。

## 開発の背景・動機

昨今、リモートワークの普及により、出社と在宅ワークのハイブリッドで働く人が増えている。そういった人にとって、定期券を購入するほど出社がない場合、定期券よりも都度電車賃を支払うきっぷのほうが交通費が安く済むというケースがある。

そこでこのサービスでは、以下の情報から**定期券の購入か都度きっぷ払いするかのどちらが金額が安く済むか**を算出できるようにする。

- 特定区間の定期券代
- きっぷ代
- 定期券を購入する期間における見込み出勤日数

---

## 主な機能

※ このセクションは開発の進行に伴い逐次更新される。

### 1. 価格比較計算

ユーザーが入力した情報をもとに、定期券ときっぷのどちらが安いかを計算する。

**入力項目：**

- 出発地
- 目的地
- 定期券の開始日
- 利用期間（1ヶ月・3ヶ月・6ヶ月のいずれか）
- リモートワークの頻度（例: 週に2回）

**出力：**

- 判定結果（定期券 or 都度払い）
- 定期券料金
- 都度払い料金
- 出勤日数

### 2. 外部APIによる料金取得

入力された区間・利用期間をもとに、以下の2つを外部APIから取得する。

- 定期券料金（定期券料金算出API）。
- 片道の切符・IC運賃（運賃計算API）。

### 3. 出勤日数の算出

開始日と利用期間から対象期間を確定し、以下を考慮して「出勤日数」を算出する。

- 土日を除外する。
- 祝日を除外する（`Yasumi`ライブラリで日本の祝日に対応）。
- リモートワーク頻度に基づいてリモートワーク日数を差し引く。

### 4. 都度払い料金の算出

```
都度払い料金 = 出勤日数 × 片道運賃 × 2（往復）
```

### 5. 判定

定期券料金と都度払い料金を比較し、安いほうを判定結果として返す。

---

## 技術スタック

| カテゴリ       | 技術                                                   |
| -------------- | ------------------------------------------------------ |
| フレームワーク | Laravel 12                                             |
| 言語           | PHP                                                    |
| フロントエンド | Blade テンプレート + Vanilla JavaScript → 最終的にReact |
| HTTP通信       | Axios                                                  |
| データベース   | MySQL                                                  |
| コンテナ       | Docker（PHP-FPM, MySQL）                               |
| デバッグ       | Xdebug                                                 |
| 祝日計算       | Yasumi（日本の祝日ライブラリ）                         |
| デプロイ先     | AWS                                                    |

---

## アーキテクチャ・ディレクトリ構成

### 設計方針

このプロジェクトでは**サービスクラスを用いたMVC**を採用する。リポジトリパターンやクリーンアーキテクチャといった大規模開発向けの構成は採用しない。

- **Controller**: リクエストの受け取りとレスポンスの返却
- **Service**: ビジネスロジックの実装
- **Model**: データベースとのやり取り（Eloquent）
- **View**: Bladeテンプレートによる画面表示

### ディレクトリ構成（主要部分）

```
commuter-pass-price-checker/
├── app/
│   ├── Http/Controllers/
│   │   └── CalcController.php    # 計算APIのコントローラー
│   └── Services/
│       └── CalcService.php       # 計算ロジックを担当するサービス
├── resources/views/
│   └── top.blade.php             # メインのUIビュー
├── routes/
│   ├── web.php                   # Webルーティング
│   └── api.php                   # APIルーティング
├── docker/
│   ├── php-fpm/                  # PHP-FPMのDocker設定
│   └── db/                       # MySQLのDocker設定
├── docs/                         # 開発ドキュメント
└── compose.yml                   # Docker Compose設定
```

---

## APIエンドポイント

### POST `/api/calc`

価格比較計算を実行する。

**リクエストボディ：**

```json
{
    "start_date": "2026-01-01",
    "ticket_period_month": 1
}
```

**レスポンス：**

```json
{
    "result": 20
}
```

<!-- TODO: レスポンスの形式は開発中のため、完成後に更新 -->

---

## 環境構築

<!-- TODO: 環境構築手順を記載 -->

```bash
# Dockerコンテナの起動
docker compose up -d

# 依存パッケージのインストール
docker compose exec app composer install

# アプリケーションキーの生成
docker compose exec app php artisan key:generate
```

---

## 関連リンク

- [Laravel ドキュメント](https://laravel.com/docs)
- [Yasumi - 祝日ライブラリ](https://github.com/azuyalabs/yasumi)
